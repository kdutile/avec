<p style="color: green"><%= notice %></p>

  <p>
    <strong>Title:</strong>
    <%= @event.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= @event.description %>
  </p>

<div>
  <%= link_to "Edit this event", edit_event_path(@event), class: "button button-primary" %> |
  <%= link_to "Back to events", events_path, class: "button button-primary" %>
</div>

<%= button_to "Destroy this event", @event, method: :delete %>
